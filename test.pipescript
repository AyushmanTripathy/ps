hello(name) => add "hello" | $name;

greet(name) => {
  log | $name 10;
  return | log "i was told to greet";
}

introduce(yourName) => {
  return | log "myself $yourName";
}

log | hello () => add 12 3;
introduce "man";

greet (a) => {
  return | add 45 $a;
}
